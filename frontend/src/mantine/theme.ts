import { createTheme } from '@mantine/core';

export const mantineTheme = createTheme({
  primaryColor: 'violet',
  primaryShade: 6,
  fontFamily: 'monospace',
  lineHeights: {
    md: '150%',
  },
});
